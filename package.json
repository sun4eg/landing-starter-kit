{
  "name": "landing-starter-kit",
  "version": "2.0.3",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "npm run test:browser:chrome",
    "test:browser": "playwright test",
    "test:browser:chrome": "PLAYWRIGHT_USE_INSTALLED_CHROME=true playwright test",
    "test:browser:headed": "PLAYWRIGHT_USE_INSTALLED_CHROME=true playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "sass": "^1.102.0",
    "vite": "^8.2.0"
  }
}
